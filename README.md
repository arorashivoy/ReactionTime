# ReactionTime

**Archived.** An Arduino reaction-time game, June 2021, written before starting
university.

A single sketch: it waits a random interval, lights an LED, and times how long you
take to hit the button. One file, `ReactionTime.ino`. The wiring is not documented and
there is no schematic — you would have to read the pin numbers out of the sketch.
